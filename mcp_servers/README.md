# MCP Servers

This directory will contain MCP (Model Context Protocol) tool servers for Phase 3.

## Planned Servers

- `filesystem.py` - File read/write operations
- `knowledge_api.py` - Knowledge base management
- `web_search.py` - Web search integration (optional)

## Usage

MCP servers are configured in `config/config.json` under `mcp_servers`.
